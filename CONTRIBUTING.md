# المساهمة | Contributing

شكرًا لاهتمامك بتطوير **سعيد ناصر — رحلة الواحة**.

## قبل البدء

1. ابحث في GitHub Issues للتأكد من عدم وجود بلاغ أو اقتراح مماثل.
2. افتح Issue يشرح المشكلة أو التحسين المقترح قبل التغييرات الكبيرة.
3. اجعل كل Pull Request مركزًا على تغيير واحد واضح.

## تشغيل المشروع

لا توجد عملية بناء. افتح `index.html` مباشرة أو شغّل:

```bash
python3 -m http.server 8000
```

## معايير التغيير

- حافظ على دعم العربية واتجاه RTL.
- اختبر لوحة المفاتيح وأزرار اللمس.
- لا تضف مفاتيح API أو بيانات شخصية أو ملفات سرية.
- حافظ على عمل اللعبة دون خادم خلفي.
- حدّث `CHANGELOG.md` عند تغيير سلوك المستخدم.
- اشرح الاختبارات اليدوية التي أجريتها في وصف Pull Request.

## التحقق من JavaScript

استخرج كتلة `<script>` من `index.html` ثم شغّل `node --check` عليها، أو اترك GitHub Actions ينفّذ الفحص تلقائيًا.

## لغة التوثيق

يمكن كتابة المناقشات بالعربية أو الإنجليزية. نفضّل توثيق التغييرات المهمة باللغتين عندما يكون ذلك عمليًا.
